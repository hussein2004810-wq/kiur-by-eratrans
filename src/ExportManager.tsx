import {useState} from 'react';import {Download,FileSpreadsheet,FileText} from 'lucide-react';import './exports.css';
type Row={attemptId:string;studentName:string;email:string;testTitle:string;subject:string;lecture:string;score:number;maxScore:number;percentage:number;finishedAt:string;universityName?:string;collegeName?:string;departmentName?:string;phaseName?:string;sectionName?:string};
async function load(){const response=await fetch('/api/admin/results',{credentials:'include'});const data=await response.json();if(!response.ok)throw new Error(data?.error?.message||'تعذر تحميل النتائج');return data.data as Row[]}function download(blob:Blob,name:string){const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=name;link.click();setTimeout(()=>URL.revokeObjectURL(link.href),1000)}
export default function ExportManager({notify}:{notify:(message:string)=>void}){const [busy,setBusy]=useState('');const run=async(type:'xlsx'|'docx'|'pdf')=>{setBusy(type);try{const rows=await load();if(type==='xlsx'){const ExcelJS=await import('exceljs');const workbook=new ExcelJS.Workbook();const sheet=workbook.addWorksheet('KIUR Results',{views:[{rightToLeft:true}]});sheet.columns=[['الطالب','studentName'],['البريد','email'],['الجامعة','universityName'],['الكلية','collegeName'],['القسم','departmentName'],['المرحلة','phaseName'],['الشعبة','sectionName'],['الاختبار','testTitle'],['المادة','subject'],['المحاضرة','lecture'],['الدرجة','score'],['الدرجة القصوى','maxScore'],['النسبة','percentage'],['تاريخ الإكمال','finishedAt']].map(([header,key])=>({header,key,width:22}));rows.forEach(row=>sheet.addRow(row));sheet.getRow(1).font={bold:true};sheet.autoFilter={from:'A1',to:'N1'};download(new Blob([await workbook.xlsx.writeBuffer()],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'}),`KIUR-results-${new Date().toISOString().slice(0,10)}.xlsx`)}else if(type==='docx'){const {Document,Packer,Paragraph,Table,TableCell,TableRow,TextRun,WidthType}=await import('docx');const header=['الطالب','البريد','الاختبار','النتيجة','التاريخ'];const tableRows=[new TableRow({children:header.map(text=>new TableCell({children:[new Paragraph({children:[new TextRun({text,bold:true})],bidirectional:true})]}))}),...rows.map(row=>new TableRow({children:[row.studentName,row.email,row.testTitle,`${row.percentage}%`,new Intl.DateTimeFormat('ar-IQ').format(new Date(row.finishedAt))].map(text=>new TableCell({children:[new Paragraph({text:String(text),bidirectional:true})]}))}))];const document=new Document({sections:[{children:[new Paragraph({children:[new TextRun({text:'ملخص نتائج منصة KIUR — ERATRANS',bold:true,size:32})],bidirectional:true}),new Paragraph({text:`تاريخ التصدير: ${new Intl.DateTimeFormat('ar-IQ').format(new Date())}`,bidirectional:true}),new Table({rows:tableRows,width:{size:100,type:WidthType.PERCENTAGE}})]}]});download(await Packer.toBlob(document),`KIUR-results-${new Date().toISOString().slice(0,10)}.docx`)}else{const container=document.createElement('div');container.className='pdfReport';container.dir='rtl';container.innerHTML=`<h1>تقرير نتائج KIUR</h1><p>منصة تابعة لقناة ERATRANS — ${new Intl.DateTimeFormat('ar-IQ').format(new Date())}</p><table><thead><tr><th>الطالب</th><th>الاختبار</th><th>النتيجة</th><th>التاريخ</th></tr></thead><tbody>${rows.slice(0,300).map(row=>`<tr><td>${escapeHtml(row.studentName)}</td><td>${escapeHtml(row.testTitle)}</td><td>${row.percentage}%</td><td>${new Intl.DateTimeFormat('ar-IQ').format(new Date(row.finishedAt))}</td></tr>`).join('')}</tbody></table>`;document.body.appendChild(container);const [{default:html2canvas},{jsPDF}]=await Promise.all([import('html2canvas'),import('jspdf')]);const canvas=await html2canvas(container,{scale:1.5,backgroundColor:'#ffffff'});container.remove();const pdf=new jsPDF({orientation:'landscape',unit:'px',format:[canvas.width,canvas.height]});pdf.addImage(canvas.toDataURL('image/jpeg',.92),'JPEG',0,0,canvas.width,canvas.height);pdf.save(`KIUR-results-${new Date().toISOString().slice(0,10)}.pdf`)}notify(`تم تصدير ${rows.length} نتيجة بنجاح`)}catch(error){notify((error as Error).message)}finally{setBusy('')}};return <section className="panel exportManager"><div><h3>تصدير النتائج</h3><p>الملفات تتضمن النتائج الواقعة ضمن نطاق صلاحيتك فقط.</p></div><button onClick={()=>void run('xlsx')} disabled={!!busy}><FileSpreadsheet/>{busy==='xlsx'?'جارٍ الإنشاء...':'Excel تفصيلي'}</button><button onClick={()=>void run('pdf')} disabled={!!busy}><Download/>{busy==='pdf'?'جارٍ الإنشاء...':'PDF رسمي'}</button><button onClick={()=>void run('docx')} disabled={!!busy}><FileText/>{busy==='docx'?'جارٍ الإنشاء...':'Word ملخص'}</button></section>}
function escapeHtml(value:string){return value.replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]!))}
